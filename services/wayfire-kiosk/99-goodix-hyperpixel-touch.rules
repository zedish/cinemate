# X' = Y,  Y' = 1 - X  — swap axes + invert Y for HyperPixel DPI touch.
# Works with wayfire transform 270 + [device:*Goodix*] output = DPI-1.
ACTION=="add|change", SUBSYSTEM=="input", ATTRS{name}=="*Goodix*", ENV{LIBINPUT_CALIBRATION_MATRIX}="0 1 0 -1 0 1"

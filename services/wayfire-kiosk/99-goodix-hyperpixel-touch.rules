# Swap axes and invert Y for HyperPixel DPI touch (normalized [0,1] space).
# X' = Y,  Y' = 1 - X  → corrects orientation with wayfire transform 270.
ACTION=="add|change", SUBSYSTEM=="input", ATTRS{name}=="*Goodix*", ENV{LIBINPUT_CALIBRATION_MATRIX}="0 1 0 -1 0 1"
